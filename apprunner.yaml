version: 1.0
runtime: python311  # Ensure this is exactly python311
build:
  commands:
    build:
      - pip install -r requirements.txt
run:
  command: python knowledge_server.py
  network:
    port: 8080
  env:
    - name: PORT
      value: "8080"

version: 1.0
runtime: python311
build:
  commands:
    build:
      # Use pip3 to ensure the command is recognized
      - pip3 install -r requirements.txt
run:
  command: python3 knowledge_server.py
